
spring:
  datasource:
    password: postgres-test-pass
    url: jdbc:postgresql://db-test:5432/server-manager-test
    username: postgres-test
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
      show-sql: true
